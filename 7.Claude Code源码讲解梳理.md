# Claude Code 上下文管理机制 — 简明梳理

---

## 一、上下文窗口是什么？为什么 agent 更容易爆？

大模型没有真正的"记忆"，每次对话都是把 **system prompt + 历史对话 + 当前问题** 一股脑塞进去。能塞进去的总 token 上限就叫**上下文窗口**。

Agent 比普通聊天更容易爆窗口，三处叠加：

- **开局固定开支大**：system prompt + 工具描述 + CLAUDE.md 就要 5k~10k token
- **工具调用双倍记账**：一次工具调用产生 `tool_use` + `tool_result` 两条消息，都进窗口
- **大文件杀伤力强**：读几个源文件，几万 token 就没了

---

## 二、Claude Code 怎么管？— 5 层压缩金字塔

设计原则：**能不压就不压，必须压时从最轻的手段开始。**

| 层级 | 名称 | 触发条件 | 做法 | API 开销 |
|------|------|----------|------|----------|
| **第 1 层** | 大结果存磁盘 | 单工具结果 > 50KB | 完整内容写磁盘，消息里只留 2KB 预览 | **零** |
| **第 2 层** | **Snip（裁剪）** | 对话头部探索性提问已无用 | 直接删掉远古消息，插入边界标记 | **零** |
| **第 3 层** | **Micro-Compact** | 距上次 API 调用 > 60 分钟 | 清空"可重新获取"的工具结果（Read/Bash/Grep 等），只保留最近 5 个 | **零** |
| **第 4 层** | **Context Collapse** | 上下文达 90%~95% | **不修改原始消息**，只在调 API 时动态计算压缩视图给模型（读时投影） | **零** |
| **第 5 层** | **Auto-Compact** | 上下文达「上限 - 13k」 | 全量重写整段对话，生成结构化摘要 | **1 次 API 调用** |

**关键设计**：5 层里 4 层零 API 开销，只有极端情况才触发第 5 层。

---

## 三、核心压缩机制：Auto-Compact（第 5 层）

### 触发时机：绝对值阈值，不是按轮数

```
触发阈值 = 模型有效上下文窗口 - 13,000 token
```

13k 是基于摘要任务 **p99.99 输出长度** 统计出来的，留了安全冗余。不管窗口扩到多大，缓冲固定不变。

### 最反直觉的设计：全量重写，不保留最近 N 条

传统做法是"保留最近 20 轮，压缩前面 180 轮"。Claude Code 的做法是：**所有历史消息，全部送进摘要器，重新写一份。**

原因：Lost in the Middle 现象——模型对上下文中间段记不清楚。与其留半模糊信息，不如全部压成结构化精华。

### 压缩后的四段式结构

```
边界标记 → 摘要消息 → 附件 → hook 结果
```

- **语义信息**（目标、决策、错误教训）→ 进摘要
- **状态信息**（文件内容、子任务进度）→ 走附件通道恢复
- **永久指令**（CLAUDE.md）→ 不进摘要，通过清空缓存让下一轮自动重加载

### 摘要 Prompt：XML + 9 部分清单

```xml
<analysis>草稿区（最终剥离）</analysis>
<summary>
  1. 主要请求和意图
  2. 关键技术概念
  3. 涉及的文件和代码段
  4. 错误和修复
  5. 解决的问题
  6. 所有用户消息（必须枚举！）
  7. 待办任务
  8. 当前工作（最细颗粒度！）
  9. 下一步建议
</summary>
```

两个灵魂项：

- **第 6 项**：用户每条消息必须枚举，不能遗漏方向变更信号
- **第 8 项**：精确到文件和函数名，不是"在调试"而是"在改 auth.ts 的 refreshToken 函数"

### 熔断保护

- **递归守卫**：压缩任务标记为 `compact`，不会再触发二次压缩，堵死死循环
- **熔断器**：连续失败 3 次就停止重试，防止 API 账单爆炸

---

## 四、压缩后怎么接续对话？

### 五步流水线

1. 所有消息 → 摘要器 → 生成摘要
2. 清空缓存（readFileState、getUserContext 等）
3. 并发生成附件（最近 5 个文件、异步任务状态、技能配置）
4. 调用 `buildPostCompactMessages` 组装新消息链
5. 新消息链替换旧链，对话继续

### 让模型无缝接活的关键设计

> 摘要开头包装为："本会话是从之前一次因上下文耗尽而中断的对话延续过来的。以下摘要概述了之前的对话内容。"

这句话告诉模型：**你不是从头开始，你是接力**。模型看到后不会傻问"请问您想做什么"，而是直接顺着 Current Work 往下干。

自动压缩时还会打开 `suppressFollowUpQuestions` 开关，禁止摘要里生成"需要进一步确认吗？"类问题，避免打断当前任务流。

**旧消息真的丢了**（除非开 Kairos 备份模式），一刀切反而干净，不需要维护两套消息状态。

---

## 五、一句话总结

> **Claude Code 的上下文管理不是"省 token"，而是"保信息结构"——不同信息有不同半衰期，语义信息走摘要、状态信息走附件、永久指令走缓存重加载，各走各的通道，互不干扰。**

---

> **参考来源：** 微信公众号"小林Coding"《Claude Code 上下文窗口管理机制 — 深度源码讲解》
