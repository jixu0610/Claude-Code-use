# Claude Code 上下文窗口管理机制 — 深度源码讲解

> **作者：** 小林（微信公众号"小林Coding"）  
> **原文链接：** https://mp.weixin.qq.com/s/NBR6dRr3iO7KCTEChk8QUg  
> **发布时间：** 2026年6月

---

## 前言

最近有位林友跟我说，他去面试某大厂 AI 岗，被面试官追问了不少 Claude Code 源码相关的问题。还好他面试前刚好啃过我公众号那两篇 Claude Code 源码分析的文章，几乎都答上来了，顺利拿下了 offer。

他说的那两篇分别是：

- 万字长文图解 Claude Code 源码：Agent工作模式、记忆机制、上下文窗口管理机制等
- 万字长文图解 Claude Code 源码：多Agent实现机制

基本把 Claude Code 比较核心的原理都剖析了一波，也收获了很多读者的好评。然后又有读者跟我反馈，想让我再深入聊聊 **「Claude Code 的上下文窗口是怎么管理的？」**

原因很简单，因为他面试被深入问到了，这可以说是 agent 开发最常考察的知识点了。只要简历上做过 agent 项目，面试官几乎都会追问一句：**你这个 agent 项目的上下文是怎么管的？**

那 Claude Code 这个被业内认证「最强 agent 范本」之一的产品，到底是怎么做的？我把 Claude Code 的 compact（压缩）相关的源码翻了好几遍，越看越觉得有意思。

**本文大纲：**

1. 什么是上下文窗口？为什么 agent 跑起来分分钟爆窗口？
2. 业界常见的上下文方案，为什么不够看？
3. Claude Code 的 5 层压缩金字塔
4. Auto-Compact 的整体思路
5. 什么时候触发压缩？压什么、留什么、丢什么？
6. 摘要 prompt 是怎么设计的？
7. 压完之后怎么接续对话？
8. 这道面试题该怎么答？

如果你能看到最后，下次面试官再问到这道题，至少能让对方眼前一亮。

---

## 一、先聊聊上下文窗口

聊 Claude Code 怎么管之前，我们先退一步，把「上下文窗口」这个概念本身搞清楚。如果你已经很熟了，可以跳过这一节直接看第二节。

### 上下文窗口是个啥？

大模型跟人不一样，它没有真正的「记忆」。每次你问它一个问题，本质上都是「从头看一遍」：把 system prompt、所有历史对话、当前问题，一股脑塞进去，然后生成回复。

塞进去的这堆东西，加起来的总长度有个上限，这个上限就叫**「上下文窗口」**。单位是 token，你可以粗略理解成「字」，但中英文有差别（中文一个字往往占 1 到 2 个 token，英文一个单词平均算 1 个 token）。

![模型工作台示意图](https://mmbiz.qpic.cn/mmbiz_png/ysyAxM1rgX3oUvWXLuPdgBWmS2EfnkibWgNN0MGVYqcZDvJsG0WFpqoxW9RV0eUre5QMmbHRFmgclLbS6FjNvib6icrt6vPVpufGArEnbBNI8s/640?wx_fmt=png&from=appmsg)

> 打个比方，上下文窗口就像是模型的「工作台面」。你能放多少东西在这张桌子上，模型就能看见多少东西。桌子的大小是固定的，超出去的东西，要么放不下，要么得挤掉一些已有的。

**当前主流模型的窗口大小：**

| 模型 | 窗口大小 | 约合字数 |
|------|----------|----------|
| GPT-4 早期版本 | 8k token | 1 万多字 |
| Claude 3.5 Sonnet | 200k token | 30 万字 |
| Claude Opus 4.7 (1M) | 1M token | 200 万字 |
| 部分 Gemini 模型 | 2M token | 400 万字 |

200 万字听起来很大对吧？拿来塞一整套《三体》三部曲都绰绰有余。但你真去做 agent，会发现这张桌子还是不够大。

### 为什么 agent 更费窗口？

普通聊天为啥不太担心窗口？因为聊天就是「你一句我一句」，每轮可能也就几十到几百个 token。一个 200k 的窗口能撑上千轮对话。

**但 agent 就不一样了，窗口压力来自三处叠加：**

**第一处，开局就是大头。** 一个 agent 开局，光是 system prompt + 工具描述 + CLAUDE.md 这些「固定开支」，就要塞进 5k 到 10k token。你还没开始干活，桌子就已经占了一角。

**第二处，工具调用是双倍记账。** 这一点很多人不知道，是上下文爆得快的元凶。当 agent 调用一个工具的时候，会产生两条消息：一条是 `tool_use`（告诉系统我要调什么工具、参数是啥），一条是 `tool_result`（工具返回了什么内容）。这两条都进窗口，都算 token。

举个例子，agent 要读 `a.py` 这个文件，对话里会变成这样：

- 第一条 `tool_use: Read("a.py")`，相当于模型说「我要调 Read 工具读 a.py」
- 第二条 `tool_result: <a.py 的完整内容>`，是系统把文件读完、把内容返回给模型

**这两条都进上下文、都算 token，一来一回，记账翻倍。**

**第三处，大文件 Read 杀伤力巨大。** agent 经常要读源代码文件，一个稍微大点的源文件几千 token 起步，一万 token 也很常见。读三五个文件，桌子上就堆满了。

**算笔账：** 一个稍微复杂点的编码 agent，开局 8k + 读 5 个平均 5k 的文件 = 33k，再加上对话和中间的 grep、edit 工具调用，跑个十几轮，几万 token 没了。长任务分分钟逼近 200k 天花板。

### 加大窗口能解决吗？

你可能会说，那模型厂商不是在卷长上下文吗？卷到 1M、2M，问题不就自然解决了？**治标不治本，三个硬伤摆在那儿：**

1. **钱**：上下文越长，单次推理 token 消耗越大，账单按几何级数上涨
2. **慢**：Attention 机制的计算复杂度跟序列长度平方相关，150k 对话可能要等十几秒
3. **Lost in the Middle（最致命）**：上下文非常长时，模型对首尾信息记得清楚，对中间段记忆模糊。这是注意力机制的固有特性，窗口再大也解决不了

![token 消耗结构图](https://mmbiz.qpic.cn/sz_mmbiz_png/ysyAxM1rgX1pu2PBqSWd9OfNS8xxRvv2CNxFcef95icCky7SyP8rrSXgca2ibY2LZybwzzxe0wfwXY9kED1xWDODZXO3nrcovxfj5BoByZPck/640?wx_fmt=png&from=appmsg)

所以你看，光靠模型厂商扩窗口救不了 agent。要救，必须 agent 自己主动管理上下文。管得好的 agent 能聊上千轮、跑通复杂任务，管不好的 agent 几十轮就开始胡言乱语、忘东忘西。

---

## 二、常见方案为什么不够看？

你去 Github 上扒一扒开源的 agent 框架，会发现上下文管理的方案大概就那么几类，我们一个一个看。

### 方案一：滑动窗口

逻辑最直白：设个阈值（比如超过 50 轮或超过 100k token），从最老的消息开始砍，保留最新的若干条。

但 agent 跑复杂任务，最关键的决策往往就在最开始。比如用户开局说「我们这次的目标是 A，注意一定要避免 B」，这种全局性指令砍掉了，后面 agent 就开始干被禁止的事，完全失控。

而且工具调用有「上下文依赖」——前面 Read 的文件内容砍了，后面引用就变成无源之水。

> **滑动窗口本质是「用遗忘换续航」。对话能聊下去，但 agent 的脑子被打了。**

![滑动窗口示意图](https://mmbiz.qpic.cn/sz_mmbiz_png/ysyAxM1rgX3sp28iaU49LJo8ShtB1QPAzeqwH146UZB3RHkcvY6Blq3Bzpl93IIuS0jujmAY3Ts1PhriceSwZK6KgEWiavEowCxCJicJ2IcdLUc/640?wx_fmt=png&from=appmsg)

### 方案二：每 N 轮做摘要

每过 10 轮或 50k token，触发一次摘要：扔给一个小模型生成总结，替换原来的消息。

问题：触发时机太死板，10 轮可能是关键节点切下去细节全丢；5 轮可能啥都没干却把对话切碎了。摘要粒度也粗，细微状态、错误修复过程、中途改的需求，全压成几句话。

> **看似在管理上下文，其实是在按节奏粗暴破坏对话连贯性。**

![每 N 轮摘要示意图](https://mmbiz.qpic.cn/mmbiz_png/ysyAxM1rgX0DfGBdMqmoDuwyVAkXzWnUVTXUWIGzo9dBwxBYYaM1Ipp0aH1jYHyibVeLMzO6HqcNiaM7OOEpZX44nQECicYUEUlLYX4IIEIU1U/640?wx_fmt=png&from=appmsg)

### 方案三：向量召回历史

把所有历史消息切片丢到向量数据库，每次用问题召回 top-k 最相关的片段塞进上下文。

这套在 RAG 系统里很普遍，但拿到 agent 场景来用会立刻翻车：

1. **agent 上下文是强时序依赖的**。向量召回不管顺序，可能 B 召回了 A 落在外面
2. **tool_use 和 tool_result 必须成对出现**，向量切片可能把这对切开
3. **recall top-k 一定会漏掉东西**，关键决策点相似度不一定高但就是关键

> **向量召回搞 RAG 检索文档行，搞 agent 上下文不行。这是两个完全不同的场景。**

![向量召回历史示意图](https://mmbiz.qpic.cn/sz_mmbiz_png/ysyAxM1rgX0xP0fqR9Kom76iaGaEOJ1cCOGCl1rV8m5OlSiaZfsiafkVU702j3yyYBofz7yr5AdYNwjhFPPibiaAspdMicZniaSylZOoGnHRWoc1P0/640?wx_fmt=png&from=appmsg)

### 三种方案对比

![三种方案对比示意图](https://mmbiz.qpic.cn/mmbiz_png/ysyAxM1rgX2kQB0Xiacyq8cTjjcegGpD4Gj96mfgr3icZFWLZzBLHjmlWVibMmCT3GbCGGBkQibKqUYO9HrDXKGPDgVBPCvZsnbpfmDPOVcfcoY/640?wx_fmt=png&from=appmsg)

你看，这三种方案各有各的硬伤。那 Claude Code 怎么干？它走的是完全不同的路子：**不是「保留加召回」，是「重写整段对话」。**

---

## 三、Claude Code 的 5 层压缩金字塔

Claude Code 的上下文管理不是一招制敌，而是一套从轻到重的 **5 层金字塔**。设计原则一句话讲清：**能不压就不压，必须压的时候从最轻的手段开始。**

![5 层压缩金字塔示意图](https://mmbiz.qpic.cn/sz_mmbiz_png/ysyAxM1rgX2xQlWeOy68ibqUp8IxtAHxaeicXrjlbPGbE2vEymME6jx1s5GNgV8Ve8yPrz4YJROPcfATSRhlx3HwKTicDOq03Cyaq7yL9hCXjw/640?wx_fmt=png&from=appmsg)

### 第 1 层：大结果存磁盘

单工具结果超 50KB → 完整内容写磁盘，消息里只留 2KB 预览。完整内容没丢，模型需要时可以再次 Read 拿回来。**零 API 开销。**

同一条消息里所有工具结果加起来还有个 200KB 总量上限，超了就挑大的存磁盘。

![大结果存磁盘示意图](https://mmbiz.qpic.cn/mmbiz_png/ysyAxM1rgX0GTu0ibJMfoOBS0uOk6Yryxvp1R4F0DzsAK7sWHuhSscBnkgyRRTBI7fNq5QnLeN1Qsk8vTA3tI3ao8Yib4b19sTo8zyWicRcQVM/640?wx_fmt=png&from=appmsg)

### 第 2 层：Snip 砍掉远古消息

对话开头那几轮探索性提问可能已经完全没用了。Snip 直接把它们删掉，插入一条「这之前的内容已被清理」的边界标记。**不做摘要、不调 API，纯本地操作，代价最低。**

Snip 释放的 token 数还会传给第 5 层 Auto-Compact，避免两层重复压缩。

![Snip 砍头部示意图](https://mmbiz.qpic.cn/sz_mmbiz_png/ysyAxM1rgX2GIQeww2x7NOv8pVOhIhh515ZMLnxxlc8ibp9tDHlibaVvwN6MjhruEyziaZ5nclOgTvVicGbSujLInenickbEoRXTVianF1Koy2IHw/640?wx_fmt=png&from=appmsg)

### 第 3 层：Micro-Compact 时间衰减

距离上一次 API 调用超过约 60 分钟时触发。逻辑是：超过这个时间，大模型 API 端的 Prompt Cache 大概率已经过期了，那不如顺手清掉旧的工具结果。

具体做法：把「可重新获取」的工具结果（Read / Bash / Grep / Glob / WebSearch / Edit / Write）清空，只保留最近 5 个。**子 agent 输出、Task 状态这类「不可重复」的结果绝不裁剪。**

![Micro-Compact 时间衰减示意图](https://mmbiz.qpic.cn/sz_mmbiz_png/ysyAxM1rgX3sicxic4ZEQ0fPQPn2W5k01TecZibll63OmrTe2ib3GGiaib17AEgNg9HRXpAqEuTYXA09jTMibJHMicuEqghrHA5wkAUBfxDg9YBAWQU/640?wx_fmt=png&from=appmsg)

### 第 4 层：Context Collapse 读时投影

上下文达到 90% 时触发，95% 时升级。这一层最巧妙：**不修改原始消息，只在调用 API 的那一刻动态计算一个压缩视图给模型。**

原对话历史在本地完整保留，模型那边看到的是压缩版。这种「写时不动、读时投影」的思路在很多数据库里也常见。

![Context Collapse 读时投影示意图](https://mmbiz.qpic.cn/sz_mmbiz_png/ysyAxM1rgX3blEQib8fsVALvA0TDdEkUJMXRM3rdHFjPWcHib1TDwia1iahk78lGqIrcYXuCMQknc3mB8pkrxeCPIayupaLlXFWuGJBLpL59sls/640?wx_fmt=png&from=appmsg)

### 第 5 层：Auto-Compact 全量摘要

最重的兜底。上下文达到 ~93%（具体是「上限减 13K」）时触发，把整段对话全送进摘要器重写一份，配套做文件恢复、缓存清理、消息重组。**代价最高（要一次 API 调用）但效果也最强。**

![Auto-Compact 全量摘要示意图](https://mmbiz.qpic.cn/mmbiz_png/ysyAxM1rgX24NUpwpDzspSybIyhVDIUJklpY94uVr5g964kumBnZWvksMMy6BJMEAxeRIEstYIXWr6ptVbo4W4jeZ4At9u4CUh0RTuhN3gg/640?wx_fmt=png&from=appmsg)

### 为什么要分 5 层？

这种「从轻到重」的设计有个隐藏的好处：**绝大部分场景根本走不到第 5 层。** 每一层都在替下一层「减负」：

- 大文件没塞进上下文 → Snip 不会触发
- Snip 已经释放了空间 → Auto-Compact 不会被激活
- Context Collapse 把上下文压到阈值以下 → Auto-Compact 同样不需要跑

**5 层里有 3 层完全零 API 开销，只在前 4 层都压不动的极端情况下，才会触发最贵的全量摘要。**

> 本文聚焦第 5 层 Auto-Compact，因为它是面试官最爱拷打的、设计最精妙的、也是最有面试加分价值的。

---

## 四、Auto-Compact 的整体思路

Auto-Compact 作为金字塔最顶层的兜底机制，核心就三件事：

1. **绝对阈值触发。** 不按轮数、不按百分比，按 token 数距离窗口上限的固定缓冲
2. **全量重写对话。** 这个最反直觉：所有历史消息，不分新旧，全部送进摘要器重新写一份
3. **关键信息走另外的恢复通道。** 文件内容、记忆文件（CLAUDE.md）、异步任务的状态，靠「重新注入」

> 异步任务：Claude Code 支持主 agent 派几个后台子 agent 同时干活，这种正在跑的子任务状态也属于关键信息，压缩时必须保住。

**打个比方：** 你是一家公司的老板，每个季度要做总结。

- ❌ 传统做法：「把所有的会议记录都翻一遍」→ 脑子里乱成一锅粥
- ✅ Auto-Compact：会议记录归档进档案室（对话里看不到了）；桌面换上一份新写的「精华版季度纪要」（摘要）；员工手册（CLAUDE.md）不动，下次还能翻；最近用过的关键文档放到桌面随手翻的位置（重新注入为附件）

**这套做法的精髓：** 它承认对话历史是会过时的。过去的细节 95% 不需要原样保留，重要的是「目标是什么、做到哪一步了、犯过哪些错、下一步该干嘛」这种**结构化的状态**。

![Auto-Compact 全景图](https://mmbiz.qpic.cn/mmbiz_png/ysyAxM1rgX0Fg16sAdJ5ibGb6YWpgCulicw93xtJlJCe9jeSYZR3mggDibsDJxDXAeD4TXKN76P3sDyPV5wDicP5LOKXczNWCk0EcIZeeEVNcPg/640?wx_fmt=png&from=appmsg)

---

## 五、什么时候触发压缩？

如果让你来设计触发时机，常见思路无非：按轮数、按比例、按时间间隔。但 Claude Code 都没用，它用的是：**距离上限的固定缓冲。**

### 阈值计算

```typescript
export const AUTOCOMPACT_BUFFER_TOKENS = 13_000

export function getAutoCompactThreshold(model: string): number {
  const effectiveContextWindow = getEffectiveContextWindowSize(model)
  return effectiveContextWindow - AUTOCOMPACT_BUFFER_TOKENS
}
```

拿到模型的有效上下文窗口（比如 200k），减去固定值 13k，得到触发阈值。token 数一旦超过这条线，就开始压。

### 为什么偏偏是 13k？

源码注释里讲了，13k 是基于模型在做摘要时的 **p99.99 输出长度**算出来的。注意是 **p99.99**，不是 p99，不是 p99.9。Anthropic 真的去跑了大规模数据统计，把摘要任务的实际输出长度分布画出来，才敢把这条线定死在 13k（实际数据约 17.3k，留安全冗余）。

![摘要输出长度的 p99.99 分布示意图](https://mmbiz.qpic.cn/sz_mmbiz_png/ysyAxM1rgX3qGVawfn2usKpkKBvu5BFict3xyXibofchwJWBl6qAcjcuicx3eGZDR5B6jxTeicXL7FpSHvUYhcxicEBaFg9OtSJ3ZpDFV1LKibowE/640?wx_fmt=png&from=appmsg)

**绝对值阈值的好处是可预测：** 不管模型窗口扩到 500k 还是 1M，触发线永远是「上限减 13k」，不会因为窗口变大就跟着膨胀。

### 手动 vs 自动触发

Claude Code 提供了手动入口 `/compact` 命令。核心压缩函数是同一个，但参数不一样：

| 模式 | 区别 |
|------|------|
| **手动** | 可传 `customInstructions`（如「请特别关注 XXX 方面」） |
| **自动** | 不接受用户指令，自动打开 `suppressFollowUpQuestions` 开关 |

`suppressFollowUpQuestions`：禁止摘要里生成「需要进一步确认」类的问题。因为自动压缩发生在 agent 干活时，不想压完了被新问题打断节奏。

![手动 vs 自动触发对比图](https://mmbiz.qpic.cn/sz_mmbiz_png/ysyAxM1rgX1wnxZtZcdJM5cmodD9oNIN7LTMBO6lfibo1rZKXOLBoal7O4GOBwKQlUj04bRrtfNU9rMDn3wibWotcHURG3vXShRC0XLEdvVlc/640?wx_fmt=png&from=appmsg)

### 熔断和递归守卫

**Circuit Breaker（电路断路器）：** 如果 auto-compact 连续失败 3 次，系统就停止重试。这个机制是踩坑踩出来的——源码注释里讲，曾经有 1000 多个会话因为反复触发压缩失败、不停重试，把 API 账单当烟花放。

![circuit breaker 熔断状态机](https://mmbiz.qpic.cn/sz_mmbiz_png/ysyAxM1rgX1ibDHLGxGJEjHPwQ5ff5baJia8K1unsQMG15PQJUFZ4MrPA85zJvGFXN6tibXsyaIiclWIEHN6q7xMLEZL8YyGwKd5u2khaCAEyC8/640?wx_fmt=png&from=appmsg)

**递归守卫：** 做摘要任务的子 agent 自己也消耗 token，会不会又触发 auto-compact 陷入死循环？不会：

```typescript
if (querySource === 'session_memory' || querySource === 'compact') {
  return false
}
```

压缩任务被标成 `compact`，会话记忆任务被标成 `session_memory`。一旦来源是这两种，直接 return false。短短三行，堵死无限递归。

![触发时机示意图](https://mmbiz.qpic.cn/sz_mmbiz_png/ysyAxM1rgX3tMWszL4SkzTGpvAIYXZ12yKEGtppoY4ibiaEiauBzqueYSJovWIgoe6N2Lyhic31DbGa2gmHiccsqicdeQErccRkbHria56AecrKVMM/640?wx_fmt=png&from=appmsg)

---

## 六、压什么、留什么、丢什么？

### 全量重写整段对话

如果让你设计压缩机制，遇到一段 200 轮的对话，你会怎么处理？

绝大多数人的直觉是：保留最近 20 轮，把前面 180 轮压成一段摘要。这也是大多数 agent 框架的做法。

**但 Claude Code 不是这么干的。它的做法非常激进：所有 200 轮，全部送进摘要器，重新写一份。**

![「直觉做法 vs Claude Code 做法」对比图](https://mmbiz.qpic.cn/sz_mmbiz_png/ysyAxM1rgX1Mbz8rlmGkcOOtiaMeIKgvrFGkMW6BLcOQodQ3TFuI5xOABDicgWnGwRrJEqib3Q4F8ZuIeYlD4ibPHZ5QW3IfavI6azgNSnPicky8/640?wx_fmt=png&from=appmsg)

第一眼看到会愣一下：最近的对话也不保留？但转念一想有道理——还记得 Lost in the Middle 吗？就算保留最近 20 轮，模型对中间几轮也看不清楚。与其留半模糊信息，不如全部压成结构化的精华。

### 压缩后的四段式结构

```typescript
export function buildPostCompactMessages(result: CompactionResult): Message[] {
  return [
    result.boundaryMarker,      // 压缩边界标记
    ...result.summaryMessages,  // 摘要消息
    ...result.attachments,      // 文件、技能、计划等附件
    ...result.hookResults,      // hook 执行结果
  ]
}
```

| 段 | 内容 | 作用 |
|----|------|------|
| 边界标记 | 自动/手动、压缩前 token、最后消息 ID | 打时间戳 |
| 摘要消息 | 全部 200 轮对话的压缩结果 | 语义核心 |
| 附件 | 最近文件、计划、技能、异步任务状态 | 状态恢复通道 |
| hook 结果 | 用户配置的 hooks 执行结果 | 自定义逻辑 |

### 不同信息有不同的「半衰期」

这套设计的妙处在于承认对话的不同信息有不同的生命周期：

| 信息类型 | 示例 | 处理方式 |
|----------|------|----------|
| **语义信息** | 「用户想加验证码」「方案改成了 JWT」 | 进摘要（一两句话即可） |
| **状态信息** | 「a.py 第 42 行有个 bug」「子任务 X 跑完了」 | 走附件（差一个字就接不上） |

![信息半衰期对照图](https://mmbiz.qpic.cn/mmbiz_png/ysyAxM1rgX3I4XGBqYfJKcxu3LWCcTc4Gd6rHtC9qqsMzwQZwibM8bMBicnfABaS1or0HhfKC0g5enHuaM0RFZNWnaQJr9iaB5Fks5qiboeNyXA/640?wx_fmt=png&from=appmsg)

> **语义信息走摘要、状态信息走附件，各管各的，效率最高。**

### Microcompact 预处理

Auto-Compact 真要跑之前，还会先做一步预处理：把对话里占大头的「工具调用结果」清空，只留元数据占位符。涉及 Read、Bash、Grep、Glob、WebFetch、WebSearch、Edit、Write。对话瞬间瘦一圈，再送进摘要器。

> 澄清：microcompact 也是第 3 层的独立机制，有自己的独立生命周期，并不是只在 Auto-Compact 时才跑。

![microcompact 前后瘦身对比图](https://mmbiz.qpic.cn/mmbiz_png/ysyAxM1rgX0xpzETtLkpq0A9MGMmKAZIZtYVpdSiaDANiajjDOkOicJ26uuicniccPBoibZkpbvCwNDDFOvOP8cn3LZNERZlRWeLFczGc8P0JqIKQ/640?wx_fmt=png&from=appmsg)

### 文件恢复：5 个、5K、50K

```typescript
export const POST_COMPACT_MAX_TOKENS_PER_FILE = 5_000
export const POST_COMPACT_TOKEN_BUDGET = 50_000
export const POST_COMPACT_MAX_FILES_TO_RESTORE = 5
```

三个常量，工程化的精华：

- 最多重新加载 **5 个文件**
- 每个文件最多 **5k token**
- 总预算不超过 **50k token**

选文件策略：按「最近活跃度」排，最近被 Read 过的优先。不管对话多复杂，文件恢复开销都是可控的。

![文件恢复策略示意图](https://mmbiz.qpic.cn/mmbiz_png/ysyAxM1rgX16XbQ5V5TPxtN39phyialhIX8oZGThhFhz4HS9l7gDWiaDhjzh1gEZoWtlFx1RlovS7f1OZH7qxiaLbrloeh61tCMiaFK4adEY4F8/640?wx_fmt=png&from=appmsg)

### CLAUDE.md 不进摘要

Claude Code 做了一个很反直觉的决策：**CLAUDE.md 不会被注入到压缩后的消息里。**

那它怎么生效？通过「清空缓存」让它在下一轮对话自动重新加载。Claude Code 内部有个 `getUserContext` 缓存，压缩时清掉，下一轮发起时发现空了，就从磁盘重新读 CLAUDE.md。

> CLAUDE.md 是「永久存活」的上下文，每一轮自动重新加载，不需要塞到压缩后的消息里占地方。「全局指令」和「当下对话」是两套机制管理，互不干扰。

![CLAUDE.md 双通道处理示意图](https://mmbiz.qpic.cn/mmbiz_png/ysyAxM1rgX2NrHjvAT0sgu7awM1X0uAEaeFbMI4OUicaWDUg6TWbZDULTtkWGn4egFjheLsJeh4qJHmQhjT6yBtxhug3ob5TiaDgR51sRkqCQ/640?wx_fmt=png&from=appmsg)

### System Prompt 和异步任务

- **System prompt** 完全不参与压缩。压缩后用 `buildEffectiveSystemPrompt` 重新构造，注入最新工具列表、权限、MCP server 列表
- **异步任务状态** 作为附件重新注入，确保主 agent 能看到子任务进度

![压缩前后消息链对比图](https://mmbiz.qpic.cn/mmbiz_png/ysyAxM1rgX343v2fNvw17aAJ1zDNdXjNfsb60DuWQalSC8Djfy1kEVWXNWtZPmTiaw1wcCib635DsuWy4TYW0LK51VU30pvvGs2hu47XbkmV4/640?wx_fmt=png&from=appmsg)

> **一句话总结：不同信息走不同通道，谁也别碰谁的地盘。**

---

## 七、摘要 prompt 是怎么设计的？

很多人觉得，让大模型写个摘要嘛，prompt 不就是「请总结一下以下对话」嘛？

**Claude Code 的摘要 prompt 长达两百多行，光是「禁止工具调用」警告就重复了两次。**

### 防呆设计：禁止工具调用

```
CRITICAL: Respond with TEXT ONLY. Do NOT call any tools.
- Do NOT use Read, Bash, Grep, Glob, Edit, Write, or ANY other tool.
- Tool calls will be REJECTED and will waste your only turn.
- Your entire response must be plain text.
```

为什么要夹着喊？因为模型很容易看到对话里提到「这个文件之前 Read 过」就手痒去 Read 一下。在普通对话里合理，做摘要时是大忌——要它总结历史，不是要它做新动作。

源码注释里讲了这是早期 Sonnet 4.6 留下的坑：那个版本经常无视一次警告，所以工程师们干脆「前后包夹」，prompt 开头讲一遍，结尾再讲一遍。

![摘要 prompt「前后包夹」示意图](https://mmbiz.qpic.cn/mmbiz_png/ysyAxM1rgX3IMPkFZyUJib3evCIq7ibrqVRzPbnZMgd9Jia7bA17Mfia941K0u7lPJ4rqKw2O0oPXKnc05CTeUGIlts0FNVOAcCKmK4HUKxib98k/640?wx_fmt=png&from=appmsg)

### 输出格式：XML + 9 部分清单

```xml
<analysis>
[模型的推理草稿，分析对话哪些重要]
</analysis>
<summary>
[结构化的摘要，按 9 个清单分块]
</summary>
```

- `<analysis>` 块是「草稿区」，让模型先想清楚再写，最终会被剥离掉
- `<summary>` 块才是真正进入对话的内容

![XML 输出结构示意图](https://mmbiz.qpic.cn/sz_mmbiz_png/ysyAxM1rgX378MmhSjqgxweftNHpmGND5L2HJyZ774G0MUyRK34Tica5gbsF2AS6mz9LenBeBVutRxNkFI1UnkKDHuUYN1xic4J408RrE6osM/640?wx_fmt=png&from=appmsg)

**必须包含 9 个固定章节：**

![摘要 prompt 结构示意图](https://mmbiz.qpic.cn/mmbiz_png/ysyAxM1rgX3X5cHicFbuicGibzV5VC4S50w7pkU8dibjbfcFIXyKwXa2FhHhmqQD6d4OEaOUfTff4DcDBnaibzjHMelQwLCj7ErBSmhGA7gdC3hE/640?wx_fmt=png&from=appmsg)

| # | 章节 | 说明 |
|---|------|------|
| 1 | Primary Request and Intent | 主要请求和意图 |
| 2 | Key Technical Concepts | 关键技术概念 |
| 3 | Files and Code Sections | 涉及的文件和代码段 |
| 4 | Errors and fixes | 碰到的错误和修复方式 |
| 5 | Problem Solving | 解决的问题 |
| 6 | **All user messages** | **所有用户消息（必须枚举）** |
| 7 | Pending Tasks | 待办任务 |
| 8 | **Current Work** | **当前正在做的事（最细颗粒度）** |
| 9 | Optional Next Step | 下一步建议 |

### 设计的灵魂：第 6 项和第 8 项

**第 6 项 — 枚举所有用户消息：** 用户在第 30 轮改了需求、第 80 轮提了新约束、第 150 轮说放弃之前的方向——这些信号是任务方向变化的关键，丢了一个 agent 就会走偏。不是「概括」，是「枚举」，一个不能落。

**第 8 项 — 最细颗粒度的当前进度：** 不是「正在调试」，而是「正在调试登录模块的 token 刷新逻辑，刚发现 cookie 过期判断有 bug，正准备改 auth.ts 文件的 refreshToken 函数」。越细，agent 接续工作越流畅。

![第 6 项 vs 第 8 项对比图](https://mmbiz.qpic.cn/mmbiz_png/ysyAxM1rgX0BP2AD4EUNdY10kxq0mpEbtuh5eG7jibh2A0wmibeXVF1bFfeK0BB9A7ywlRPvZn7kibTIAvpLjicVZyXCAFTm3VuFMqDacmzWE0E/640?wx_fmt=png&from=appmsg)

### 摘要用什么模型？

**Claude Code 用的是当前对话的同一个模型（Opus 4 或 Sonnet 4），不省钱换小模型。** 两个原因：

1. **摘要质量要保证** — 便宜小模型丢东西多，下一轮 agent 接不上。摘要是 agent 接续工作的「灵魂文件」
2. **Prompt Cache 复用** — 主对话本来就在用 prompt cache，压缩用同一个模型能复用 system prompt 部分的 cache，省下来的钱比换小模型省的还多

---

## 八、压完之后怎么接续对话？

### 压缩流水线五步走

1. 所有消息 → 摘要器 → 生成摘要文本
2. 清空各种缓存（readFileState、loadedNestedMemoryPaths、getUserContext）
3. 并发生成附件（最近文件、异步 agent 状态、技能配置等）
4. 调用 `buildPostCompactMessages` 组装新消息链
5. 新消息链替换旧链，对话继续

用户看到的只有一句轻飘飘的「Compacted」，然后 agent 接着干活。

### 旧消息真的丢了吗？

**真的丢了。** 除非打开 Kairos 模式（transcript 备份机制），旧消息会写到本地 transcript 文件里，可以事后回查。

听起来暴力，但明智。如果允许「回滚」，整个对话状态会很复杂，要维护两套消息，token 也省不下来。**一刀切，反而干净。**

![旧消息「真丢了」示意图](https://mmbiz.qpic.cn/sz_mmbiz_png/ysyAxM1rgX1UIVfRiapQH9icj4cTpXJkfE4KiaOpwUINygOicyD1fFfhhatBj2DsI6jhwlzfUJE2QPJIAtfFmicBeBwIopubxBNZGqjc1EZt2GLY/640?wx_fmt=png&from=appmsg)

### 让模型无缝接活的小心思

摘要的开头会被包装成：

> 「本会话是从之前一次因上下文耗尽而中断的对话延续过来的。以下摘要概述了之前的对话内容。」

这句话告诉模型：你不是从头开始的，你是接着干。模型看到这，就不会傻乎乎地问「请问您想做什么」，而是直接顺着 Current Work 往下接活。

摘要末尾还会带一个 transcript 文件路径，给 agent 一个「翻底牌」的兜底通道。

`suppressFollowUpQuestions` 开关只在 auto-compact 时打开，禁止摘要器生成「需要进一步确认」类的问题，避免打断当前任务流。手动 `/compact` 时关闭，因为用户主动触发就是干预动作。

![压缩接力流程图](https://mmbiz.qpic.cn/mmbiz_png/ysyAxM1rgX1ia4KdCrloRMGLyRXN8RsHd0LKEqrDPiaLO8XfEKyCeJpNGBnm3Z2eg3B4XfibpQJFrBwAxQmxRsRcial6PzkJ9PVTxBiaqldicsPIc/640?wx_fmt=png&from=appmsg)

---

## 九、面试该怎么答？

下次面试官再问你「Claude Code 的上下文窗口是怎么管理的？」，用下面这套节奏：

### 一句话亮观点

> 「Claude Code 用的不是滑动窗口、定期摘要、向量召回那一类常见方案，而是一种叫『全量重写加分通道恢复』的工程化思路。」

这一句话本身就比 90% 的候选人讲得有结构感了。

### 四层铺细节

![面试话术四层结构卡片图](https://mmbiz.qpic.cn/sz_mmbiz_png/ysyAxM1rgX2dLDP4eGOh3IaQ3HhmTrzDZzuAXMBF2weYg9gfQW1SSDbrWEYm4BhibEYBGibv3IKWK93ibibMWmrELHGXJV8lCvCwFXW3e07rhkA/640?wx_fmt=png&from=appmsg)

**第一层：触发时机。** 用的是绝对 token 阈值，公式是「有效上下文窗口减去 13k 缓冲」。13k 是基于摘要任务 **p99.99** 输出长度算出来的。这一层讲完，面试官就知道你是真翻过源码。

**第二层：取舍逻辑。** 反直觉的点是它不保留最近 N 条，而是把所有历史消息一刀切全部送进摘要器重写一份。关键状态信息单独走附件通道恢复：最近读过的 5 个文件（每个最多 5k token，总预算 50k）、异步任务状态、当前计划文件。CLAUDE.md 这种永久指令不进摘要，通过清空 `getUserContext` 缓存让下一轮自动重新加载。

**第三层：摘要 prompt 设计。** 用 9 部分结构化清单约束输出，重点强调「所有用户消息」必须枚举不能落、「当前正在做的事」要精确到文件和函数名。摘要用同一个模型，不省钱换小的，保证质量并复用 prompt cache。

**第四层：接续机制。** 压缩后清空文件状态缓存、并发生成附件、用 `buildPostCompactMessages` 组装新消息链。摘要外包一句「本会话是从之前一次因上下文耗尽而中断的对话延续过来的」，让模型知道自己是接力不是从头开始。自动触发时打开 `suppressFollowUpQuestions` 避免打断当前任务。

### 一句话收口

> 「Claude Code 这套设计反映的不是『省 token』的小聪明，而是『信息分通道管理』的工程哲学。」

这句话留给面试官细品，整段回答就稳了。节奏一分半到两分钟，面试官想追问任意一层你都能展开：

- 13k 怎么来的？p99.99 统计
- 9 部分清单是哪 9 部分？
- circuit breaker 怎么防止烧钱？连续失败 3 次熔断

---

## 写在最后

回头看 Claude Code 的这套压缩机制，最戳人的是一句话：

> **上下文管理不是「省 token」，是「保信息结构」。**

很多 agent 项目把上下文压缩当成一个性能优化问题来做，逻辑是「窗口要爆了，赶紧砍点东西」。但 Claude Code 不是这么想的，它把上下文管理当成 agent 的**「灵魂工程」**：意图、进度、错误教训、待办、用户每一句话，这些才是 agent 能接续工作的本钱。token 数只是表面，保住这些结构化信息才是核心。

做 agent 时太容易陷入「这个 token 多了、那个 prompt 长了」的细节优化，反而忘了思考**「我的 agent 到底依赖哪些上下文信息才能干好活」**这个本质问题。Claude Code 用一套精细的分层管理告诉你答案：**不同信息有不同的半衰期，要分别管理。**

### 开放思考

当未来上下文窗口扩到 1 亿 token，是否还需要 compaction？

**我的预测是：仍然需要。** Lost in the Middle 现象不是上下文窗口大小的问题，是注意力机制的固有特性。窗口再大，中间段的信息照样会被模型「看不清楚」。所以无论窗口怎么膨胀，**主动管理信息结构这件事永远不会过时。**

Anthropic 这套机制对所有做 agent 的同学来说，都是教科书级的范本。

---

## 推荐阅读

- 万字长文图解 OpenClaw 面试题
- 万字长文图解 Harness 工程
- 万字长文图解 RAG 面试题
- 万字长文图解 GraphRAG 和 LightRAG
- 万字长文图解 Agent 面试题
- 万字长文图解 Claude Code 入门
- 万字长文图解 Claude Code 源码：Agent工作模式、记忆机制、上下文窗口管理机制等
- 万字长文图解 Claude Code 源码：多Agent实现机制

## 面试突击资源

| 资源 | 地址 |
|------|------|
| 小林图解网站 | xiaolincoding.com |
| 简历制作网站 | jianli.xiaolinnote.com |
| 刷题闯关+模拟面试 | 牛面Offer小程序 |
| 后端训练营 | Java/Go 后端训练营 |
| 社招面试辅导 | 后端+AI社招私教辅导 |
| Agent训练营 | 转行去做Agent开发了 |
| AI开发项目 | AI Agent 项目 |

---

> **原文来源：** 微信公众号"小林Coding"  
> **原文链接：** https://mp.weixin.qq.com/s/NBR6dRr3iO7KCTEChk8QUg

