# Agent 应用开发基础梳理

> 来源：学习记录笔记整理 | 更新时间：2026-06-11

---

## 一、提示词工程

### 1.1 结构化提示词

核心公式：**角色 + 任务 + 约束 + 输出格式**

- **角色**：准确设定模型身份（如"你是一名资深 Java 后端工程师"）
- **任务**：清晰描述要做什么
- **约束**：限制条件（字数、范围、禁止事项等）
- **输出格式**：JSON、Markdown 等，配合 Few-shot 示例提升稳定性

### 1.2 Few-shot 少样本提示

在 Prompt 中给出 1~3 个输入→输出的示例，显著提升**意图识别准确率**和**输出格式稳定性**。

### 1.3 高阶优化方法

| 方法 | 核心思路 | 适用场景 |
|------|----------|----------|
| **思维链（CoT）** | 将问题拆解为逻辑步骤，"请一步一步思考" | 复杂推理、数学题 |
| **思维树（ToT）** | 探索多条推理路径，前瞻评估选最优解 | 多方案决策 |
| **ReAct** | 思考→行动→观察→总结，循环迭代 | Agent 自主规划 |

### 1.4 提示词哲学

> 提示词设计如炼丹 — 不存在"完美配方"，需结合模型能力、参数规模、任务难度、上下文窗口等因素，持续实验、观察、调整。

---

## 二、开发框架

### 2.1 Spring AI

**Java 生态的 LLM 应用开发框架**，核心组件：

#### 核心概念

```
ChatModel → ChatClient（封装） → Advisors（AOP 链）
```

| 组件 | 说明 |
|------|------|
| **ChatModel** | 底层模型调用接口，封装 Prompt / Message / ChatOptions |
| **ChatClient** | 对 ChatModel 的进一步封装，提供流式 API |
| **ChatResponse** | 封装模型返回结果 |
| **Tools** | 声明可用工具，LLM 自主决策调用 |
| **Advisors** | 类似 AOP，通过 `getOrder()` 串联执行链，处理前置/后置逻辑 |

#### 核心特性

- **流式输出**：基于 SSE 实现
- **结构化输出**：`BeanOutputConverter` 生成格式约束拼入 Prompt → 模型返回 JSON → 自动转实体。封装为 `.call().entity(Book.class)`
- **对话记忆**：`ChatMemory` + `ChatMemoryRepository`，支持内存/数据库持久化
- **Advisor 链**：`ChatModelCallAdvisor`、`SimpleLoggerAdvisor`、`SafeGuardAdvisor`、`MessageChatMemoryAdvisor` 等

#### 本地模型调用

- **Ollama**：Docker 部署，`ollama pull deepseek-r1:7b`，通过 Ollama 提供的 API 对话
- **百炼**：阿里云模型服务平台，申请 API Key 即可接入

### 2.2 LangChain4j

**Java 版 LangChain**，分两个 API 层级：

#### 低层次 API（手动控制）

```java
// 普通对话
OpenAiChatModel.chat(ChatRequest)

// 流式输出
OpenAiStreamingChatModel.chat(String, StreamingChatResponseHandler)

// 工具调用（Function Call）
chatRequest 传入工具列表 → LLM 返回工具名+参数 → 程序执行 → 结果封装回 Messages → 再次发给 LLM 总结
```

| 能力 | 做法 |
|------|------|
| **对话记忆** | 两种 ChatMemory：按轮次限制 / 按 token 限制 |
| **结构化输出** | chatRequest 拼入 `output in json format` + 设置 `responseFormat` |
| **工具调用** | `@Tool` 注解方法 + `@P` 注注入参 |

#### 高层次 API（`@AiService`）

```java
@AiService  // 注解接口 → 自动生成 Bean
interface Assistant {
    String chat(String message);
    Flux<String> chatStream(String message);  // 流式
    Book extractBook(String text);            // 结构化输出
}
```

**实现原理（三步）：**

1. **扫描与元数据收集**：扫描 `@AiService` 注解，获取接口类及注解参数
2. **注册生产说明书**：注册 `AiServiceFactory`，存入配置（用哪个 Model / Memory）
3. **运行时懒生产**：注入时通过 JDK 动态代理生成 Bean，拦截方法调用转为 LLM API 请求

**持久化记忆**：实现 `ChatMemoryStore` 接口 → `ChatMemory` → `ChatMemoryProvider` 三层。

### 2.3 框架对比

| 维度 | Spring AI | Spring AI Alibaba | LangChain4j |
|------|-----------|-------------------|-------------|
| **生态** | Spring 官方 | 阿里云百炼集成 | 社区活跃 |
| **上手** | 中等 | 低（阿里云深度适配） | 低 |
| **高层封装** | ChatClient | DashScope 适配 | @AiService |
| **适用** | 通用 Java 项目 | 阿里云用户 | 多模型切换需求 |

---

## 三、Function Call 与 MCP

### 3.1 Function Call

**流程**：用户输入 → LLM 判断需调用工具 → 返回工具名+参数 → 程序执行 → 结果反馈 LLM → LLM 总结输出

**定义好 Tool 的要点：**

- 方法和参数描述要**清晰具体**
- 参数**扁平化**、数量不宜过多
- 用**枚举**限定参数取值范围
- 配合 **Few-shot 示例**
- 避免复杂工具调用链（结果不会自动传递）

**Spring AI 实现方式：**

```
方式一：现有服务封装 → 定义 record（入参/出参） → 配置类 @Bean 返回 Function<入参, 出参>
方式二：新工具 → @Tool 声明方法 + @ToolParam 描述参数 → chatClient.prompt().tools(new TimeTools())
```

**调用原理**：ChatClient 底层 `OpenAiChatModel.call()` → 拼接 Tools → 模型返回工具调用 → `ToolCallingManager` 自动执行 → **递归**请求 LLM 总结（`returnDirect()=false` 时继续，`=true` 时直接返回）

### 3.2 MCP（Model Context Protocol）

**一句话定义**：MCP 是 LLM 获取外部工具能力的**标准化协议**，让模型能调用外部服务。

#### 技术原理

| 层 | 说明 |
|----|------|
| **数据层** | JSON-RPC 协议；四阶段：初始化 → 工具发现 → 调用 → 变更通知 |
| **传输层** | Stdio（子进程标准输入输出）/ SSE（读写分离双连接）/ Streamable HTTP（单连接分块传输） |

#### 三种传输方式

| 方式 | 特点 |
|------|------|
| **Stdio** | MCP Server 在本机，通过子进程标准输入输出通信 |
| **SSE** | 读通道长连接（接收推送），写通道短连接（发送请求），异步返回 |
| **Streamable HTTP** | 单连接，POST Body 承载 JSON-RPC，分块传输编码实时写入响应 |

#### Spring AI 集成

**MCP Server 开发**：用 Spring AI Studio 或手写，支持 HTTP SSE / Streamable HTTP。

**MCP Client 调用**（两种方式）：

```
自动注入：配置文件中配置 → McpSyncClient → SyncMcpToolCallbackProvider → ToolCallback[] → ChatClient
手动构建：手动创建 MCPClient → SyncMcpToolCallbackProvider 封装 → ToolCallback[] → ChatClient
```

**调用链路**：ChatClient → ChatModelAdvisor → ChatModel.call() → 拼接工具列表 → 模型返回工具调用 → ToolCallback 实例调用 MCP Server → 结果递归回 LLM 总结。

#### 工程实践要点

- **重连**：初始失败 retry + 定时 ping 失败 retry（需重建 MCPClient 和 ChatClient）
- **HTTPS 改造**：MCP Server 升级为 HTTPS
- **认证鉴权**：远程服务通过请求头 Token 区分用户；本地服务无法区分多用户
- **context-path**：建议不要设置，两端都存在兼容问题
- **工具过滤**：按需筛选 MCP 工具列表
- **跳过总结**：`returnDirect()=true` 跳过 LLM 二次总结

#### MCP vs A2A

| 协议 | 定位 |
|------|------|
| **MCP** | 智能体 ↔ 外部工具（能力获取） |
| **A2A** | 智能体 ↔ 智能体（对话协调） |

---

## 四、RAG（检索增强生成）

### 4.1 RAG 核心流程

```
文档读取 → 数据清洗 → 文档分片 → 向量化 → 存入向量数据库 → 检索 → 拼接 Prompt → LLM 生成
```

### 4.2 文档分片

| 分片方式 | 说明 |
|----------|------|
| **固定大小分块** | 按固定 token 数切割 |
| **递归分块**（常用） | 按分隔符层级（段落→句子→词）递归切 |
| **基于文档分块** | 按文档自然结构（章/节）切 |
| **语义分块**（常用） | 按语义边界切分 |
| **父子分片** | 检索时用小粒度子块，召回后用父块补全上下文 |

Spring AI 使用 `TokenTextSplitter`；Spring AI Alibaba 提供递归分片；LangChain4j 支持语义分段。

### 4.3 向量存储

```
向量模型（Embedding） → 向量数据库（PGvector / ES 等） → 相似度搜索
```

常用相似度算法：KNN、IVF、HNSW、LSH。

### 4.4 检索增强生成（两种方式）

| 方式 | 做法 |
|------|------|
| **手动拼接 Prompt** | 检索文档 → 手动拼入提示词 → 调用 LLM |
| **QuestionAnswerAdvisor** | 传入 VectorStore + SearchRequest + PromptTemplate，自动代理 |

核心思路一致：检索相关文档 → 拼入提示词 → LLM 生成回答。

### 4.5 RAG 优化技术

| 技术 | 说明 |
|------|------|
| **元数据过滤** | 插入时附加元数据，检索时按元数据过滤 |
| **问题改写** | 分解 / 富化 / 多样化 / 回溯原始问题 |
| **查询路由** | 数据源路由（LLM 判断查哪个库）、Prompt 路由 |
| **查询构造** | Text → SQL（用户问题 + 表结构 + 日期 → SQL 语句） |
| **问题澄清** | 主动反问用户，获取更多信息 |
| **HyDE** | 先让 LLM 生成假设答案，用假设答案去检索真实文档 |
| **混合检索** | 关键词（ES）+ 语义向量，并行多路召回 + RRF 排序融合 |
| **重排序** | RRF 算法 / ReRank 模型对召回结果精排 |
| **Graph** | 图数据库查询实体关系 |

---

## 五、Skills

### 5.1 Skills 是什么

Skills 是 Agent 的**可复用能力模块**。每个 Skill 包含：

- `skills.md`：元数据 + 具体指导（提示词流程）
- `reference/`：参考资料
- `script/`：可执行脚本

### 5.2 Skills 使用流程

```
框架粗筛（关键词/向量检索 skills.md 元数据） → LLM 精选（理解语义挑最匹配的 Skill） → 上下文加载 → LLM 思考决策 → 框架执行工具 → 结果反馈 LLM → LLM 按输出契约格式化交付
```

本质是一个 **ReAct 动态循环**：框架做粗筛，LLM 做精判，协同执行，格式化输出。

### 5.3 工具可用性保障（四层机制）

| 层级 | 机制 | 说明 |
|------|------|------|
| **1** | 静态依赖声明 | `skills.md` 元数据声明依赖的 MCP/脚本，加载时预检，缺失则拒绝加载 |
| **2** | 运行时健康检查 | 定期 Ping，工具列表动态刷新，断线自动隐去 |
| **3** | 环境隔离 | 脚本打包进 Docker，消除环境差异 |
| **4** | 降级兜底 | 工具缺失时降为"智囊模式"，打印参数引导用户手动操作 |

### 5.4 如何创建 Skill

利用 `create-skills` 技能，给出具体要求和范围，自动生成 Skill 骨架，根据需要修改即可。

---

## 六、Agent

### 6.1 核心理解

> Agent = LLM（大脑） + Tools（手脚） + 观察反馈（眼睛）→ ReAct 循环

**ReAct Agent 流程**：Thought（思考） → Action（调用工具） → Observation（观察结果） → 循环直到输出最终答案。

### 6.2 常用 Agent 架构

| 架构 | 特点 |
|------|------|
| **ReAct Agent** | 思考→行动→观察循环，Spring AI / Spring AI Alibaba 均有实现 |
| **Plan-and-Execute** | 先规划再执行，规划→执行→批判→压缩→总结 |
| **Reflection Agent** | 自我反思，对输出结果进行自我批评和改进 |
| **Human-in-the-Loop** | 关键节点人工介入审批 |
| **Multi Agent** | 多智能体协作，通过 A2A 协议通信 |

### 6.3 ReAct Agent 核心组件

| 组件 | 作用 |
|------|------|
| **System Prompt** | 定义角色、行为边界 |
| **Model** | LLM 大脑 |
| **Tools** | 可调用工具集（MCP / 插件 / 脚本） |
| **结构化输出** | 输出格式化 |
| **Memory** | 持久化记忆 |
| **Hooks** | 生命周期钩子 |
| **Interceptors** | 请求拦截（敏感词防控、动态提示词注入） |

### 6.4 Plan-and-Execute 流程

```
规划阶段（生成 Task 列表）
  → 执行阶段（按 TaskOrder 执行，相同优先级并行，结果存 AllState；每个 Task 本身也是 ReAct Agent）
  → 批判阶段（判断是否可总结，不能则给出下一轮建议）
  → 压缩阶段（裁剪冗余对话和重复推理，保留关键信息）
  → 总结阶段（汇总输出）
```

### 6.5 A2A（Agent-to-Agent 协议）

Google 提出的多智能体通信协议，核心概念：

| 概念 | 说明 |
|------|------|
| **A2A Client** | 发起请求方 |
| **A2A Server** | 提供服务方 |
| **Agent Card** | 智能体能力名片（JSON 格式描述能力、接口、认证等） |
| **Task** | 分配的任务单元 |
| **Message** | 智能体间通信消息 |
| **Artifact** | 任务产出物 |

---

## 七、知识体系一览

```
提示词工程 ──→ 结构化 Prompt / CoT / ReAct / Few-shot
     │
开发框架 ──→ Spring AI（ChatClient + Advisor + Tools）
     │       LangChain4j（底层 API + 高层 @AiService）
     │
工具调用 ──→ Function Call（Tool 定义与递归调用）
     │       MCP（JSON-RPC + Stdio/SSE/Streamable HTTP）
     │
知识增强 ──→ RAG（分片 → 向量化 → 检索 → 增强生成 → 排序优化）
     │
能力封装 ──→ Skills（元数据声明 + LLM 决策 + 工具保障 + 降级兜底）
     │
智能体 ───→ Agent（ReAct / Plan-Execute / Multi-Agent / A2A）
```

---

> **参考来源**：个人学习记录 `Agent开发学习.txt`
