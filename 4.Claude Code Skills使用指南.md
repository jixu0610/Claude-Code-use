# Claude Code Skills 使用指南

> 最后更新：2026-06-10 | 共 27 个技能

---

## 一、内置技能（13 个）

以下为 Claude Code 自带技能，无需安装，开箱即用。

| 技能 | 调用方式 | 说明 |
|------|----------|------|
| deep-research | `/deep-research` | 深度调研：多源搜索 → 交叉验证 → 生成引用报告 |
| update-config | `/update-config` | 配置 settings.json（权限、环境变量、hooks 等） |
| keybindings-help | `/keybindings-help` | 自定义键盘快捷键 |
| verify | `/verify` | 运行应用验证代码改动是否生效 |
| code-review | `/code-review` | 审查 diff：正确性 bug + 复用/简化/效率优化 |
| simplify | `/simplify` | 仅做代码质量优化（不查 bug，用 code-review 查） |
| fewer-permission-prompts | `/fewer-permission-prompts` | 扫描历史记录，添加常用命令白名单，减少权限弹窗 |
| loop | `/loop` | 定时循环执行任务（如 `/loop 5m /check-deploy`） |
| claude-api | `/claude-api` | Claude API / SDK 参考：模型、定价、参数、token 计数等 |
| run | `/run` | 启动项目应用并验证改动 |
| init | `/init` | 初始化项目的 CLAUDE.md 文档 |
| review | `/review` | 审查 Pull Request |
| security-review | `/security-review` | 对当前分支变更进行安全审查 |

---

## 二、skill-creator（1 个）

技能创建工具，用于编写、测试和优化自定义技能。

| 技能 | 调用方式 | 说明 |
|------|----------|------|
| skill-creator | `/skill-creator` | 创建新技能、改进已有技能、运行 evals 测试、优化 description 触发准确度 |

**核心流程：**
1. 明确技能用途和触发场景
2. 编写 SKILL.md 草稿
3. 创建测试用例（2-3 个真实 prompt）
4. 并行运行测试（带技能 vs 无技能对比）
5. 生成评估报告（`generate_review.py`）
6. 根据反馈迭代改进
7. 优化 description 触发精度（可选）
8. 打包发布

**存储位置：** `~/.claude/skills/skill-creator/`

---

## 三、Superpowers（14 个）

Superpowers 是一套开发方法论技能集，覆盖从需求到交付的完整开发流程。存储于 `~/.claude/skills/superpowers.*/`。

### 需求 & 设计阶段

| 技能 | 调用方式 | 说明 |
|------|----------|------|
| brainstorming | `/superpowers.brainstorming` | **创意工作前必须使用**：探索用户意图 → 设计方案 → 获得批准后再编码 |
| writing-plans | `/superpowers.writing-plans` | 将需求/规格转为多步骤实施计划，零上下文假设，任务粒度化 |

### 实现阶段

| 技能 | 调用方式 | 说明 |
|------|----------|------|
| subagent-driven-development | `/superpowers.subagent-driven-development` | 子代理驱动开发：分发独立任务并行执行，内置代码审查 |
| executing-plans | `/superpowers.executing-plans` | 在新会话中按计划逐步执行，含审查检查点 |
| dispatching-parallel-agents | `/superpowers.dispatching-parallel-agents` | 面对 2+ 个独立任务时并行分发 |
| test-driven-development | `/superpowers.test-driven-development` | 实现功能/bugfix 前先写测试（Red-Green-Refactor） |
| using-git-worktrees | `/superpowers.using-git-worktrees` | Git Worktree 隔离开发环境 |

### 调试 & 质量

| 技能 | 调用方式 | 说明 |
|------|----------|------|
| systematic-debugging | `/superpowers.systematic-debugging` | 遇到 bug/测试失败/异常行为时，先系统性分析再修复 |
| requesting-code-review | `/superpowers.requesting-code-review` | 完成任务/实现功能/合并前请求代码审查 |
| receiving-code-review | `/superpowers.receiving-code-review` | 接收审查反馈后，先验证再实施修改 |
| verification-before-completion | `/superpowers.verification-before-completion` | 完成前验证：确认所有改动符合预期 |

### 交付 & 扩展

| 技能 | 调用方式 | 说明 |
|------|----------|------|
| finishing-a-development-branch | `/superpowers.finishing-a-development-branch` | 实现完成、测试通过后，选择合并/PR/清理策略 |
| writing-skills | `/superpowers.writing-skills` | 编写和测试新的 Superpowers 技能 |
| using-superpowers | `/superpowers.using-superpowers` | Superpowers 使用指南（含各平台适配说明） |

---

## 目录结构概览

```
~/.claude/
├── settings.json          # 配置文件（env, enabledPlugins, hooks 等）
└── skills/                # 自定义技能目录
    ├── skill-creator/     # 技能创建工具
    ├── superpowers.brainstorming/
    ├── superpowers.dispatching-parallel-agents/
    ├── superpowers.executing-plans/
    ├── superpowers.finishing-a-development-branch/
    ├── superpowers.receiving-code-review/
    ├── superpowers.requesting-code-review/
    ├── superpowers.subagent-driven-development/
    ├── superpowers.systematic-debugging/
    ├── superpowers.test-driven-development/
    ├── superpowers.using-git-worktrees/
    ├── superpowers.using-superpowers/
    ├── superpowers.verification-before-completion/
    ├── superpowers.writing-plans/
    └── superpowers.writing-skills/
```

## 常用命令

| 命令 | 作用 |
|------|------|
| `/skills` | 浏览全部可用技能 |
| `/reload-skills` | 重新加载技能（修改技能后使用） |
| `/plugin` | 管理插件市场 |
| `<技能名>` | 直接调用技能 |
