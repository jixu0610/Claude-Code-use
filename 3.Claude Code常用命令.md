# Claude Code 常用命令速查表

> 更新时间：2026-06-10

---

## 一、会话管理

| 命令 | 说明 |
|------|------|
| `claude` | 启动 Claude Code（同目录下有未完成会话会提示恢复） |
| `claude --resume` / `claude -r` | 恢复最近一次会话 |
| `claude -p "提示词"` | 非交互模式，直接执行一条提示（适合脚本调用） |
| `claude "文件名"` | 将文件内容作为提示词输入 |
| `/resume` | 列出历史会话，选择恢复 |
| `/clear` | 清空当前会话上下文（保留配置和权限），开始新话题 |
| `Ctrl + C` | 中断当前生成 |
| `Ctrl + D` | 退出 Claude Code |
| `/exit` | 退出 Claude Code |

---
######   

### 会话存储的三层结构

  ① history.jsonl — 用户命令索引

  每行一条用户输入摘要

  不包含任何 AI 回复。 用途：/resume 命令弹出的历史列表、命令搜索（Ctrl+R）。

---

  ② projects/<项目路径>/<sessionId>.jsonl — 这才是真正的完整对话转录

  ef3aee93-...jsonl  360KB（当前会话，仍在增长中）

  每一行都是完整事件：

  {"type":"system", "subtype":"local_command", "content":"<command-name>/skills</command-name>..."}
  {"type":"user", "content":"我要复习Agent开发..."}
  {"type":"assistant", "content":"现在我已了解..."}
  {"type":"tool_call", "tool":"Read", "content":...}
  {"type":"tool_result", ...}

  包含： 用户消息、AI 回复、工具调用、工具结果、系统事件、权限选择——整个会话的完整流水账。

---

  ③ sessions/<PID>.json — 进程注册表

  不包含任何对话内容。它是一个进程 → 会话的映射表，文件名是 PID，内容里指向 sessionId。

---



###   继续对话的工作流程

  /resume 命令
      │
      ▼
  ① 读取 history.jsonl → 展示历史会话列表（只有用户说了什么）
      │
      ▼
  ② 用户选择某个会话 → 根据 sessionId 去 projects/<项目>/<sessionId>.jsonl
      │
      ▼
  ③ 完整加载 jsonl 中每一条记录 → 重建对话上下文
      │
      ▼
  ④ 同时更新 sessions/<当前PID>.json → 标记新进程正在接管该 sessionId

---



## 二、模型与配置

| 命令 | 说明 |
|------|------|
| `/model` | 查看 / 切换当前使用的模型 |
| `/config` | 打开配置界面（主题、模型、权限等可视化配置） |
| `/status` | 查看当前会话状态（模型、Token 用量、项目等信息） |
| `/cost` | 查看本次会话 Token 消耗与费用估算 |
| `/context` | 查看 / 管理上下文窗口使用情况 |
| `/fast` | 切换 Fast Mode（使用 Opus 模型更快输出） |
| `/thinking` | 切换思考模式（可开关扩展推理） |

---

关于思考模式：

- **快捷键（`Alt + T`）**：是**实时开关**。在对话过程中，随时可以一键开启或关闭思考模式。
- **思考力度（`Effort Level`）**：是**思考上限**。不仅可以在启动时指定，在对话过程中也能随时通过输入 **`/config`** 命令进行修改（下一轮对话立即生效）。

**结论**：你可以用快捷键控制“要不要思考”，用 `/effort` 命令控制“思考得有多深”，两者在过程中都可以自由调整。



## 三、代码与文件操作

| 命令 | 说明 |
|------|------|
| `/init` | 初始化项目，自动生成 CLAUDE.md（项目文档） |
| `/review` | 代码审查——审查当前分支的变更（PR 级别） |
| `/code-review` | 代码审查——针对当前 diff 检查 bug 和优化点 |
| `/simplify` | 代码简化——对变更代码做复用、简化、效率优化（不查 bug） |
| `/security-review` | 安全审查——对当前分支变更做安全评审 |
| `/verify` | 验证代码变更是否按预期工作（运行 + 行为检查） |
| `/run` | 启动并驱动项目应用，验证改动效果 |
| `/add-dir` | 将目录添加到工作区（让 Claude 能访问该目录文件） |
| `/ide` | IDE 集成相关操作 |
| `! <shell命令>` | 在当前会话的终端中执行命令（结果直接返回到对话） |

---



## 四、工作流与自动化

| 命令 | 说明 |
|------|------|
| `/loop <间隔> <命令>` | 定时循环执行命令（如 `/loop 5m /review` 每5分钟审查代码） |
| `/loop` | 进入自主循环模式，自行控制节奏 |
| `/schedule` | 管理定时任务 |
| `/workflows` | 查看 / 管理多智能体工作流（Workflow） |
| `/agents` | 查看当前可用的子代理类型 |
| `ultracode` 模式 | 启用后用 Workflow 编排多智能体处理每个实质性任务 |

---



## 五、Skills（技能）

Skills 是可复用的能力封装单元（提示词 + 工具 + 输出格式），可通过 `/skill名` 调用。

| 命令 | 说明 |
|------|------|
| `/skills` | 列出所有可用 Skills |
| `/<skill名>` | 调用指定 Skill |
| `/create-skills` | 创建自定义 Skill |

### 常用内置 Skills

| Skill | 功能 |
|-------|------|
| `init` | 初始化项目 CLAUDE.md |
| `code-review` | 代码审查（diff → bug + 优化） |
| `simplify` | 代码简化（仅优化，不查 bug） |
| `review` | PR 代码审查 |
| `security-review` | 安全审查 |
| `verify` | 验证代码变更 |
| `run` | 运行应用验证 |
| `deep-research` | 深度调研（多源搜索 → 验证 → 综合报告） |
| `loop` | 定时循环任务 |
| `claude-api` | Claude API / Anthropic SDK 开发参考 |
| `keybindings-help` | 自定义键盘快捷键 |
| `update-config` | 配置 settings.json（权限、钩子、环境变量） |
| `fewer-permission-prompts` | 减少权限弹窗（自动配置白名单） |

---



## 六、MCP（Model Context Protocol）

| 命令 | 说明 |
|------|------|
| `/mcp` | 查看 / 管理 MCP 服务器（连接、断开、状态） |
| `/mcp add` | 添加 MCP Server |
| `/mcp remove` | 移除 MCP Server |
| `/mcp list` | 列出已连接的 MCP Server 及其工具 |

MCP 配置示例（`settings.json` 中配置或通过 `/mcp` 交互添加），支持三种传输方式：
- **Stdio**：本地子进程（标准输入输出流）
- **SSE**：双连接（读长连接 + 写短连接）
- **Streamable HTTP**：单连接（POST Body 承载 JSON-RPC）

---



## 七、插件（Plugins）

| 命令 | 说明 |
|------|------|
| `/plugin` | 查看插件列表与管理 |
| `/plugin install <名称>` | 安装插件 |
| `/plugin uninstall <名称>` | 卸载插件 |
| `/plugin marketplace` | 浏览插件市场 |

---



## 八、权限与安全

| 命令 | 说明 |
|------|------|
| `/permissions` | 查看 / 管理工具调用权限（允许/拒绝/白名单） |
| `/security-review` | 对当前变更做安全审查 |

权限层级：
1. **全局设置**（`~/.claude/settings.json`）——作用于所有项目
2. **用户设置**（`~/.claude/settings.local.json`）——用户级本地覆盖
3. **项目设置**（`<项目>/.claude/settings.json`）——仅作用于本项目
4. **项目本地**（`<项目>/.claude/settings.local.json`）——项目级本地覆盖（不提交 git）

---



## 九、记忆系统

| 命令 | 说明 |
|------|------|
| `/memory` | 查看 Claude Code 关于你和你项目的记忆 |
| 在对话中自然提及 | Claude 会自动判断是否需要持久化记忆（如偏好、项目约定） |

记忆存储位置：`~/.claude/projects/<项目路径>/memory/`

---



## 十、其他实用工具

| 命令 | 说明 |
|------|------|
| `/help` | 显示帮助信息 |
| `/doctor` | 诊断环境问题（依赖、配置等） |
| `/update` | 检查并更新 Claude Code |
| `/todos` | 查看当前会话的任务列表 |
| `/stats` | 查看使用统计 |
| `/export` | 导出当前会话为 Markdown 文件 |
| `/bashes` | 查看后台运行的 Shell 任务 |
| `/tasks` | 查看 / 管理后台任务 |
| `# 文本` | 以注释方式输入（不会被发送给 LLM） |

---



## 十一、键盘快捷键

| 快捷键 | 说明 |
|--------|------|
| `Ctrl + C` | 中断当前生成 |
| `Ctrl + D` | 退出 Claude Code |
| `Ctrl + L` | 清屏 |
| `Ctrl + R` | 搜索历史命令 |
| `Enter` | 发送消息（可配置） |
| `Shift + Enter` | 换行 |
| `Esc` | 取消当前操作 / 关闭弹窗 |

> 自定义快捷键：`/keybindings-help` 或在 `~/.claude/keybindings.json` 中配置。

---



## 十二、CLI 启动参数速查

```bash
claude                      # 交互模式启动
claude -r                   # 恢复最近会话
claude --resume             # 同上
claude -p "一句话任务"       # 非交互模式（单次执行）
claude -c "多行指令"         # 同上（-c 和 -p 等价）
claude --model <模型名>     # 指定模型启动
claude --verbose            # 详细日志模式
claude --version            # 查看版本
claude --help               # 查看所有参数
```

---



## 十三、项目配置示例（settings.json）

```json
{
  "permissions": {
    "allow": [
      "Bash(npm run *)",
      "Bash(git: *)"
    ],
    "deny": []
  },
  "env": {
    "NODE_ENV": "development"
  },
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [{ "type": "command", "command": "echo 'about to run command'" }]
      }
    ]
  }
}
```

---



## 附录：目录结构速查

```
~/.claude/                        ← 全局目录
├── settings.json                 ← 全局配置（模型、权限、环境变量）
├── history.jsonl                 ← 所有对话命令历史
├── sessions/                     ← 活跃会话状态快照（用于恢复）
├── projects/                     ← 按项目隔离的元数据
├── plugins/                      ← MCP 插件市场缓存
├── file-history/                 ← 文件编辑历史版本（撤销/重做）
├── backups/                      ← settings.json 历史备份
└── cache/                        ← 通用缓存

<项目>/.claude/                   ← 项目级目录
├── settings.json                 ← 项目配置（权限白名单等）
├── settings.local.json           ← 本地覆盖配置（不提交 git）
├── CLAUDE.md                     ← 项目文档（/init 生成）
├── scheduled_tasks.json          ← 定时任务（/loop 创建的持久化任务）
└── memory/                       ← 持久化记忆文件
```
