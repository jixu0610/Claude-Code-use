# Claude Code 安装与模型路由 — 简明梳理

> 更新时间：2026-06-11

---

## 一、Claude Code 安装

### 1.1 前置环境要求

| 依赖 | 最低版本 | 推荐版本 |
|------|----------|----------|
| **Node.js** | 18.0.0 | 22.x LTS |
| **npm** | 9.0.0 | 10.x |
| **Git** | 2.30+ | 2.43+ |

### 1.2 三种安装方式

#### 方式一：npm 全局安装（通用）

```bash
npm install -g @anthropic-ai/claude-code
claude --version    # 验证安装
```

> 无 Node.js 环境可先用 nvm 安装：
> ```bash
> curl -o- https://raw.githubusercontent.com/nvm-sh/nvm/v0.40.1/install.sh | bash
> nvm install 22 && nvm use 22
> ```

#### 方式二：原生脚本安装（官方推荐，无需 Node.js）

**macOS / Linux / WSL：**
```bash
curl -fsSL https://claude.ai/install.sh | bash
```

**Windows PowerShell：**
```powershell
irm https://claude.ai/install.ps1 | iex
```

---

## 二、CC Switch 切换到 DeepSeek / GLM

CC Switch 是社区开源桌面工具，让你在 Claude Code 中一键切换到国产大模型（DeepSeek、GLM 等），本质是通过修改 `~/.claude/settings.json` 配置文件中的 API 端点来实现路由。

### 2.1 安装 CC Switch

| 平台 | 方式 |
|------|------|
| **macOS** | `brew tap farion1231/ccswitch && brew install --cask cc-switch` |
| **Windows** | [GitHub Releases](https://github.com/farion1231/cc-switch/releases/latest) 下载 `.msi` 或 `.zip` |
| **Linux** | 下载 `.deb` / `.rpm` / `.AppImage`；Arch：`paru -S cc-switch-bin` |

### 2.2 切换到 DeepSeek

打开 CC Switch → 选择顶部应用栏 **Claude Code** → 点击「添加供应商」→ 选择 DeepSeek，填写：

```
Base URL：https://api.deepseek.com/anthropic
API Key：sk-你的DeepSeek密钥
主模型：  deepseek-v4-pro[1m]      ← 100万 token 上下文版本
快速模型：deepseek-v4-flash
```

点击「启用」→ **重启 Claude Code 终端** → 输入 `你是什么模型？` 验证。

> DeepSeek 专门提供了 `/anthropic` 兼容端点，使 Claude Code 的 API 请求无需额外转换即可直达。

### 2.3 切换到智谱 GLM

同理，添加智谱 GLM 供应商，填写：

```
Base URL：https://open.bigmodel.cn/api/anthropic
API Key：你的智谱 API Key
主模型：  glm-4-7  或  glm-5.1
```

启用后重启终端即可生效。

### 2.4 原理

CC Switch 本质上修改的是三个环境变量：

```
ANTHROPIC_BASE_URL    → 把 API 请求从 api.anthropic.com 改到 DeepSeek/GLM
ANTHROPIC_AUTH_TOKEN  → 替换成对应厂商的 API Key
ANTHROPIC_MODEL       → 告诉 API 用哪个模型
```

Claude Code 本身的 `/model` 命令仍然可用，但切的是厂商侧模型（如 deepseek-v4-pro → deepseek-v4-flash）。**上下文不丢，随时可切。**

---

## 三、一句话总结

> **安装**：`npm install -g @anthropic-ai/claude-code` 一行搞定，或用原生脚本免 Node.js 部署。
> **接国产模型**：装 CC Switch 选 DeepSeek/GLM 一键启用，重启终端即生效。
